idempotency:{jobId}
lock:job:{jobId}

job:active:{jobId}

device:printer:{id}
device:laser:{id}
device:plc:{id}
device:camera:{id}

device_state:{deviceId}

dashboard:summary

session:{token}

online_users

mqtt:last_message

outbox:publisher_lock